spring:
  application:
    name: demo

  datasource:
    url: jdbc:mysql://localhost:3306/likelion_pbl
    username: root
    password: dami0819!

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true

management:
  endpoints:
    web:
      exposure:
        include: "*"
